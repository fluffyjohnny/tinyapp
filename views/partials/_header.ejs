<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-primary">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a> 
      </div>
    </div>

        <% if (user && !undefined) { %>
          <form class="form-inline" method="POST" action="/logout">
            <a class='nav-item'>Logged In As: <%= user.email %> !</a> &nbsp&nbsp&nbsp
            <button type="submit" class=" btn btn-dark">Logout</button>
          </form>
        <% } else { %>

        <form class="form-inline" method="POST" action="/login">
          <input name="username" id="submit" type="text" placeholder=" Username"> &nbsp&nbsp&nbsp
          <button type="submit" class=" btn btn-dark">Login</button> &nbsp&nbsp&nbsp
        </form>
        <form class="form-inline" method="GET" action="/register">
          <button type="submit" class=" btn btn-success">Register</button>
        </form>
        <% } %>

  </nav>
</header>
